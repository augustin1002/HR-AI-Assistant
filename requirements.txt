streamlit>=1.35
pandas>=2.0
python-dotenv>=1.0

# RAG
pypdf>=4.0
scikit-learn>=1.3          # TF-IDF fallback embeddings (no download needed)
sentence-transformers>=2.6 # optional, better embeddings (downloads a model)
faiss-cpu>=1.8              # optional, needed only with sentence-transformers

# LLM providers (install the one(s) you use)
openai>=1.30
anthropic>=0.30
requests>=2.31              # used for the Ollama HTTP API

# Optional: real MCP server exposing these tools to MCP clients
mcp[cli]>=1.0
